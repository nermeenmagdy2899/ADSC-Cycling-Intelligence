# Server-side only. Never expose this value through a VITE_ prefixed variable.
OPENAI_API_KEY=

# Optional model overrides used by the local and deployed API proxy.
OPENAI_ASSISTANT_MODEL=gpt-5-mini
OPENAI_INSIGHTS_MODEL=gpt-5-mini
